# 멀티에이전트 주식 투자 시스템

한국 주식(KOSPI200 유니버스) 대상 멀티에이전트 투자 판단 시스템.
LLM 분석가들이 각자 의견을 내고, 결정론적 리스크 게이트가 최종 승인한다.

작업 시작 전 `docs/PLAN.md`를 읽을 것. 이 파일은 규칙만 담고, 배경과 근거는 그쪽에 있다.

**수정하거나 특이사항을 발견할 때마다 `docs/CHANGELOG.md` 맨 위에 날짜와 함께 남길 것.**
코드 수정뿐 아니라 EC2 크론탭·상태 파일처럼 커밋에 안 남는 운영 변경도 반드시 여기 쓴다.
2026-08-19 매수 시각을 09:00→09:01로 옮긴 조치가 git 밖에 있었던 탓에 하루 만에
출처 불명이 됐다 — 그 재발을 막는 파일이다.

---

## 왜 이런 제약이 있는가

이 프로젝트의 이전 버전은 실패했다. 원인은 코드 품질이 아니라 설계였다.

스케줄러가 "pending 매수 신호가 5개 미만이면 분석을 다시 실행"하는 구조였고, 이것이
사실상 **일일 매수 할당량**으로 작동했다. 좋은 후보가 없는 날에도 재분석을 반복했고,
같은 종목을 반복해서 보여주니 결국 통과했다. 문턱을 낮춘 적은 없지만 재시도가 문턱을
대신 낮췄다.

그 결과 매일 매수가 발생했고, **신호에 예측력이 있는지 자체를 측정할 수 없게 됐다.**
좋아서 산 건지 채워야 해서 산 건지 구분이 불가능했기 때문이다.

아래 절대 규칙은 전부 이 실패의 재발을 막기 위한 것이다. 편의를 위해 우회하지 말 것.

---

## 절대 규칙

1. **기본 상태는 현금이다.** 매수 없음이 정상 출력이며, 실패나 예외가 아니다.
2. **목표 신호 개수 파라미터를 만들지 않는다.** `target_signals`, `min_positions`,
   `top_n` 같은 것을 도입하지 말 것. 상위 N개 선택 금지.
3. **절대 문턱으로만 거른다.** 후보가 0개인 날이 있어야 정상이다.
4. **재시도는 데이터 수집 실패에만 허용한다.** API 타임아웃·파싱 에러는 재시도 OK.
   분석은 성공했는데 결과가 마음에 안 들어서 다시 돌리는 것은 금지. 이 둘은 코드
   경로가 분리되어야 한다.
5. **LLM에게 종목 비교를 시키지 않는다.** 후보 여러 개를 한 프롬프트에 넣고 "이 중
   고르라"고 하면 모델은 반드시 뭔가를 고른다. 종목당 독립 호출로 "이 종목이 기준을
   충족하는가"를 판정한다. 다른 후보 정보는 컨텍스트에 넣지 않는다.
6. **최종 승인권은 코드가 갖는다.** LLM은 거부권만 가진다. 리스크 게이트를 LLM으로
   구현하지 말 것.
7. **실거래 연결 금지.** 마일스톤 3까지는 모의투자로만 검증한다. 실거래 전환은 사용자가
   명시적으로 지시할 때만.

---

## 아키텍처

```
[수집: 코드]      시세·지표(한투·pykrx)   뉴스(RSS·크롤러)   공시(DART API)
                        |                      |                  |
[분석: LLM]        차트 분석가            뉴스 분석가         공시 분석가
                        |                      |                  |
                        +----------------------+------------------+
                                               |
[판단: LLM]                          강세/약세 토론  (반대 논거 강제 생성)
                                               |
                                    포트폴리오 매니저 (근거 종합)
                                               |
[게이트: 코드]                   리스크 게이트 (한도 초과 시 무조건 거부)
                                               |
[집행: 코드]                              주문 집행
```

수집과 게이트/집행은 결정론적 코드다. LLM을 넣지 말 것.
분석가는 데이터를 직접 가져오지 않는다. `MarketContext`로 주입받는다.

---

## 핵심 계약

`src/schemas.py`에 두고, 여기서 벗어난 형태로 계층 간 데이터를 주고받지 않는다.
에이전트끼리 자연어 문자열로 대화하게 만들지 말 것.

```python
class AnalystOpinion(BaseModel):
    agent: str                       # "chart" | "news" | "disclosure" | "quant"
    ticker: str
    score: float                     # -1.0 ~ 1.0
    confidence: float                # 0.0 ~ 1.0
    evidence: list[str]              # 원문 ID + 프롬프트 버전 ("prompt:chart@a3f2c1")
    as_of: datetime                  # 이 판단이 사용한 데이터의 최신 시점

class Decision(BaseModel):
    ticker: str
    action: Literal["BUY", "HOLD"]   # 매도는 별도 경로
    reason: str
    inputs: list[AnalystOpinion]     # 이 결정을 만든 의견 전체
    degraded: bool                   # 분석가 일부가 실패한 상태에서 나온 결정인가

class GateResult(BaseModel):
    approved: bool
    rejected_by: str | None          # "position_limit" | "sector_concentration" | ...
```

**분석가는 `AnalystOpinion | None`을 반환한다.** "데이터 부족으로 판단 불가"와
"판단했으나 점수가 낮음"은 다른 상태다. 뉴스가 없는 종목에 score=0을 주면 상위 계층이
중립 의견으로 오해한다. 없는 의견은 없는 것으로 남길 것.

**`evidence`에 프롬프트 버전을 반드시 포함한다.** 나중에 성능 변화가 프롬프트 때문인지
데이터 때문인지 구분할 유일한 단서다.

---

## 파일 구조

처음에는 납작하게 간다. 빈 폴더를 채우려고 불필요한 추상화를 만들지 말 것.

```
src/
  schemas.py      # 계약 (처음부터 분리)
  llm.py          # Claude 래퍼 — 재시도·타임아웃·스키마 검증·토큰 로깅
  collectors.py   # 수집 전부
  analysts.py     # 분석가 전부
  pipeline.py     # 토론 → 매니저 → 게이트 → 집행
prompts/          # 프롬프트는 코드 밖에. .md 파일로.
tests/
deploy/crontab   # 운영 크론 원본. EC2에서 crontab -e로 직접 고치지 않는다
docs/PLAN.md
docs/CHANGELOG.md  # 변경·특이사항 기록 (날짜 필수)
```

**쪼개는 시점은 파일이 길어졌을 때가 아니라, 한 파일을 서로 다른 이유로 고치기 시작했을
때다.** 예: `analysts.py`를 어떤 날은 DART 파싱 때문에, 어떤 날은 차트 프롬프트 때문에
열게 되면 그때 분리한다. 구현체가 하나뿐인데 `base.py`에 Protocol을 만들지 말 것.

프롬프트를 파이썬 문자열에 박지 않는다. 별도 파일이어야 프롬프트 수정 커밋만 따로 남고,
성능 변화의 원인을 추적할 수 있다.

---

## 기술 스택

- Python 3.11+, pydantic v2, asyncio
- Anthropic API 직접 호출. **LangGraph / CrewAI / AutoGen 사용하지 않는다.**
  이 시스템은 에이전트 간 대화나 동적 툴 호출이 없고 병렬 호출 후 스키마 취합이
  전부라서, 프레임워크는 디버깅만 어렵게 만든다.
- 분석가 호출은 `asyncio.gather(..., return_exceptions=True)`.
  DART가 죽어도 차트 분석가는 돌아야 한다. 단, 분석가가 빠진 채 나온 결정은
  `Decision.degraded=True`로 표시하고 게이트에서 기준을 높인다.
- 테스트는 pytest. LLM 호출은 고정 응답으로 목킹하고, 파이프라인 로직은 네트워크 없이
  전부 테스트 가능해야 한다.

---

## 구축 순서 — 아래에서 위로

다이어그램의 아래쪽(게이트·집행)부터 만든다. 분석가부터 만들면 결과가 나오는 순간
"이 종목 사볼까"가 되고 게이트는 계속 뒤로 밀린다. 이전 프로젝트가 무너진 지점이다.

**마일스톤 1 — 아무것도 사지 않는 시스템**
분석가 자리에 랜덤 점수 더미를 넣고 게이트·집행·로깅을 완성한다.
완료 조건:
- 20영업일치를 돌렸을 때 매수 신호가 난 날이 절반 이하
- 게이트 거부 사유가 `rejected_by`별로 집계되어 로그에 남음
- 같은 입력을 두 번 넣으면 같은 결정이 나옴 (재현성)

**마일스톤 2 — 분석가 투입**
차트 분석가 → 뉴스 분석가 → 공시 분석가 순으로 하나씩 붙인다.
붙일 때마다 신호 발생률이 어떻게 변하는지 기록한다.

**마일스톤 3 — 측정**
IC(information coefficient, 예측값과 실제 수익률의 순위상관)를 측정한다.
0.03~0.05가 안정적으로 나오면 쓸 만한 신호다.
**백테스트 수익률을 성공 지표로 삼지 말 것.** LLM은 과거 뉴스 이후 무슨 일이
있었는지 이미 학습해서 알고 있어 과거 구간 성능이 부풀려진다. LLM 컴포넌트는
워크포워드·실시간 모의투자로만 검증한다.

---

## 감시 지표

대시보드나 일일 로그에 반드시 포함할 것:

- **최근 20영업일 중 매수 신호가 난 날의 비율** — 매일 신호가 나면 시스템이 고장 난
  것이다. 관망일이 절반 이상이어야 정상에 가깝다.
- 게이트 거부 사유별 건수 — 특정 룰만 계속 발동하면 그 룰이 과하거나 분석가가 한쪽으로
  쏠려 있다는 뜻이다.
- 분석가별 호출 수·실패율·토큰 사용량

---

## 아직 정해지지 않은 것

임의로 정하지 말고 사용자에게 물어볼 것:

- 리스크 게이트 룰의 구체적 수치 (종목당 최대 비중, 섹터 집중도, 일일 손실 한도,
  총 노출)
- 매도 로직 — 결정론적 안전장치는 확정·구현됨(`src/sell.py`, docs/PLAN.md §5).
  문턱은 고정값이 아니라 **진입 시점에 매니저가 종목별로 정하고 청산까지 얼린다**
  (`ExitPlan`, 2026-08-15 확정). 손절 3~15% 범위, 익절은 항상 그 2배(코드가 강제),
  익절 비중 15~60%, 트레일링 3~12%. LLM 재량 매도(보유 종목 재평가 기반)는 아직
  미정 — 다음 단계에서 사용자와 다시 정할 것.

  **보유 중에는 이 문턱을 절대 재결정하지 않는다.** 편의를 위해 "보유 종목도
  다시 평가해서 손절선을 조정" 같은 경로를 만들지 말 것 — 이미 손실 중인 포지션
  앞에서 물으면 모델은 반드시 넓힐 이유를 만들어낸다. 재시도가 문턱을 대신 낮췄던
  실패(위 서두)와 같은 모양이다.
- 정량 ML 분석가(`quant`) 도입 여부. 마일스톤 3에서 IC가 낮게 나온 뒤에 검토한다.
  타깃은 가격 방향 예측이 아니라 횡단면 랭킹 / 변동성 / 이벤트 후 이상수익률 중 하나.
